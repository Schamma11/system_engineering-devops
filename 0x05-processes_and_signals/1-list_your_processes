#!/usr/bin/env bash
# Display all running processes in human-readable format with hierarchy

echo "USER       PID %CPU %MEM    VSZ   RSS TTY      STAT START   TIME COMMAND"
ps -eo user,pid,%cpu,%mem,vsz,rss,tty,stat,start,time,cmd --sort=pid

echo -e "\nProcess Hierarchy:"
pstree -p -a